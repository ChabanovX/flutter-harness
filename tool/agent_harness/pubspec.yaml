name: agent_harness
description: Local architecture, verification, and scaffolding harness for Flutter projects.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  analyzer: 13.3.0
  args: ">=2.5.0 <3.0.0"
  glob: ^2.1.3
  path: ">=1.9.0 <2.0.0"
  yaml: ">=3.1.2 <4.0.0"

dev_dependencies:
  test: ">=1.25.0 <2.0.0"
  very_good_analysis: ^10.2.0
